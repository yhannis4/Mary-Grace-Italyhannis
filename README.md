# Mary-Grace-Italyhannis

to be changed 

current files added are brute force and greedy first fit decreasing algorithm

main approach:
for brute force, it tries every item, backtracks for other branches
greedy FFD: first sorts the items (i used bubble sort) each item is iterated, makes a decision; if it fits, place it there immediately, if not; new bin

brute force : always optimal combinations/solutions; runtime exponentially increases on larger on inputs
FFD greedy : usually, but not always ; larger inputs have low runttime
